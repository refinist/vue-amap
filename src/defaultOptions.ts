import type { DefaultOptions } from './types';

export const defaultOptions: DefaultOptions = {
  width: '100%',
  height: '100%',
  loadOptions: {
    key: '',
    version: '2.0'
  },
  mapOptions: {},
  hideLogo: false
};
